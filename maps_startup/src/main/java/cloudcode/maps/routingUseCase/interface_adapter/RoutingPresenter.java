package cloudcode.maps.routingUseCase.interface_adapter;

import cloudcode.maps.routingUseCase.use_case.RoutingOutputBoundary;
import cloudcode.maps.routingUseCase.use_case.RoutingOutputData;

public class RoutingPresenter implements RoutingOutputBoundary {

    @Override
    public void prepareSuccessView(RoutingOutputData routes) {
    }
}
